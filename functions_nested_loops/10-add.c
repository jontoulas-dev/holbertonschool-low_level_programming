#include "main.h"
#include <stdio.h>

/**
 * add - Print table of 9
 *@a: premier entier
 *@b: deuxieme entier
 * Return: resulat de a plus b
 */
int add(int a, int b)
{
int result;

result = a + b;

return (result);
}
