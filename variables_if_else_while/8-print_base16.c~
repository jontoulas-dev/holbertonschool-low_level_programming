#include <stdio.h>

/**
 * main - Print alphabet
 *
 * Return: Always 0
 */
int main(void)

{
int i;
for (i = '0'; i <= 'f'; i++)
{
putchar(i);

if (i > '9' && i < 'a')
{
i = 'a';
}
}
putchar('\n');
return (0);
}
