#include <stdio.h>

/**
 * main - Print alphabt
 *
 * Return: Always 0
 */

int main(void)
{
char lettre = 'a';

while (lettre <= 'z')
{
if (lettre != 'e' && lettre != 'q')
{
putchar(lettre);
}
lettre++;
}
putchar('\n');
return (o);
}
